doctype html
html(lang="en")
  head
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    title User
  body
    div.container
      h1.mt-4.mb-3 User
      div.card
        div.card-body
          h5.card-title Name: #{user.username}
          p.card-text Email: #{user.email}
          p.card-text ID: #{user.id}
